# Sub-6a A3: submit_order_bulk gate measurement

Auto-generated by `scripts/measure-bulk-submit-gates.ts`. Each run appends a timestamped block.

## Decision (A5 carry-forward)

**Threshold:** 350,000 gates (+25% over Sub-5a clearing baseline 281,594, vk_hash `2aae33dd4ea01690`).

- [ ] If N <= 350,000: KEEP MAX_ORDERS_PER_BULK = 9
- [ ] If N > 350,000: DOWNSIZE MAX_ORDERS_PER_BULK to 5; re-measure; update this section.

**Outcome (2026-05-23, A5):** **PROVISIONAL KEEP at 9; measurement pending.**

Rationale: A3's measurement script ships ready-to-run (`scripts/measure-bulk-submit-gates.ts`) but the dev box lacks the `bb` CLI from Aztec's toolchain. The first real measurement is an operator follow-up. Until then Sub-6a defaults to `MAX_ORDERS_PER_BULK = 9` (1 real + 8 decoys); if the operator's first measurement exceeds 350K, they re-run A5 to downsize to 5 + commit the change.

Rough estimate from per-call gate cost analysis: each `_submit_one_order_internal` call performs validation (~5K gates) + pool registry walk (~MAX_NUM_POOLS × per-pool-cost ~15K) + 1 cross-contract Token transfer (~10K) + 1 OrderNote insert (~3K) + encrypted delivery (~7K) = ~40K gates per slot. 9 slots × 40K ≈ 360K — borderline, may require downsize at A5 measurement.

**Operator follow-up to run A5 properly:**
1. Install `bb` CLI via `aztec-up` toolchain.
2. Ensure `nargo compile` from `contracts/orderbook/` produces a fresh target (em-dash blockers cleared in commit `582e951`).
3. Run `pnpm tsx scripts/measure-bulk-submit-gates.ts` from repo root.
4. Read the measurement block appended to this file.
5. If N > 350K: change `global MAX_ORDERS_PER_BULK: u32 = 9;` to `5` in `contracts/orderbook/src/main.nr` + update the A2 tests' array sizes from `[...; 9]` to `[...; 5]` + re-measure + update this section's Outcome line.

## Measurement runs

_(populated by `scripts/measure-bulk-submit-gates.ts`)_
